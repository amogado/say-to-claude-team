# Status — Afficher l'etat de la queue

## Action

```bash
bash <SKILL_DIR>/scripts/status.sh
```

Afficher le resultat a l'utilisateur. Si des sessions mortes sont detectees, proposer `/say-to-claude-team gc`.
